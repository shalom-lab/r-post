name: Deploy Console to GitHub Pages

on:
  push:
    branches: [main, master]
    paths:
      - "apps/console/**"
      - "content/**"
      - "prompt-rules/**"
      - "topics/**"
      - ".github/workflows/pages.yml"
      - "package.json"
      - "package-lock.json"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm install

      - name: Sync content into console public
        run: node scripts/sync-public.mjs

      - name: Build
        working-directory: apps/console
        run: npm run build

      - name: Ensure .nojekyll for Pages
        run: touch apps/console/dist/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/console/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
