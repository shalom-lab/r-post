# DeepSeek — 本地 generate 脚本；Actions 用仓库 Secret DEEPSEEK_API_KEY
DEEPSEEK_API_KEY=

# 禁止把 PAT 写成 VITE_*（会打进浏览器包）。
# 操作台只用本机 localStorage：gh-repo-rpost / gh-token-rpost
# 推荐 fine-grained PAT：Contents Read/Write + Actions Write（workflow 触发）
