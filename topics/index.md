# RPost 候选选题

> 选题数据由 topics/index.json 管理；本页自动生成，用于阅读。

## 001. 用 grepl() 从州名中筛出目标文本

- 状态：候选
- 分类：r-base
- 成稿：
- 创建：2026-09-21
- 示例数据：datasets::state.name；R 内置的 50 个美国州名字符向量。

### 选题说明

用 R 内置的 state.name 演示最基本的模式匹配，得到逻辑结果并筛出含 New 的州名。

### 内容大纲

1. **先看字符向量**：查看 state.name 的前几项，明确 grepl() 要搜索的对象。
2. **用 grepl() 判断是否匹配**：搜索 New，观察 TRUE/FALSE 结果，并解释 pattern 与 x 两个参数。
3. **把匹配结果用于筛选**：用逻辑向量取出完整州名，核对数量和结果。

### 读者带走

grepl() 返回逻辑值，最适合直接参与筛选。

## 002. 正则里的 ^ 和 $ 到底匹配哪里？

- 状态：候选
- 分类：r-base
- 成稿：
- 创建：2026-09-21
- 示例数据：datasets::state.abb；R 内置的 50 个美国州名缩写字符向量。

### 选题说明

用 state.abb 检查州缩写的开头和结尾，直观看懂字符串边界，而不是死记符号。

### 内容大纲

1. **先找以 A 开头的缩写**：使用 ^A 匹配字符串开头，列出并核对结果。
2. **再找以 A 结尾的缩写**：使用 A$ 匹配字符串结尾，与上一组结果并排比较。
3. **组合边界做完整校验**：用 ^[A-Z]{2}$ 检查缩写是否恰好由两个大写字母组成。

### 读者带走

^ 锁定开头，$ 锁定结尾，两者合用可以校验完整字符串。

## 003. sub() 和 gsub() 有什么区别？

- 状态：候选
- 分类：r-base
- 成稿：
- 创建：2026-09-21
- 示例数据：文章内定义的最小可复现字符向量，例如带重复横线和空格的标签；用于说明替换语义。

### 选题说明

用带有多余空格和横线的最小文本示例，对比只替换第一次与替换全部匹配。

### 内容大纲

1. **准备一组有重复符号的文本**：展示原始字符向量，并明确希望统一清理的部分。
2. **用 sub() 只替换第一次**：执行一次替换，观察每个字符串中仍然保留的后续匹配。
3. **用 gsub() 替换全部**：对同一输入执行全局替换，与 sub() 的结果逐项比较。

### 读者带走

sub() 每个字符串只处理第一次匹配，gsub() 会处理全部匹配。

## 004. 如何从文件名中提取年份和编号？

- 状态：候选
- 分类：r-base
- 成稿：
- 创建：2026-09-21
- 示例数据：文章内定义的最小可复现文件名字符向量，例如 report_2024_03.csv；不对应外部文件。

### 选题说明

从 report_2024_03.csv 这类文件名中提取数字片段，练习 gregexpr() 与 regmatches()。

### 内容大纲

1. **看清文件名的共同结构**：列出几个文件名，标出需要提取的连续数字。
2. **定位全部数字片段**：用 gregexpr() 和 [0-9]+ 找到每个字符串中的多个匹配。
3. **取出匹配结果**：把位置结果交给 regmatches()，核对年份和编号是否完整。

### 读者带走

gregexpr() 负责找位置，regmatches() 负责取出实际文本。

## 005. 用捕获组调整双词州名的顺序

- 状态：候选
- 分类：r-base
- 成稿：
- 创建：2026-09-21
- 示例数据：datasets::state.name；R 内置州名字符向量，其中包含 New York、Rhode Island 等双词名称。

### 选题说明

从 state.name 找出 New York 等双词州名，再用捕获组改成 York, New，理解括号和反向引用。

### 内容大纲

1. **先筛出双词州名**：用空格作为线索找出双词名称，先确认实际输入。
2. **用括号捕获两个单词**：构造两个捕获组，并解释每组保存了哪一段文本。
3. **在替换结果中调整顺序**：用 sub() 和反向引用生成“第二词, 第一词”，逐项核对变化。

### 读者带走

捕获组不仅用于匹配，还能在替换文本中重复使用匹配到的内容。

## 006. 用 mtcars 三分钟看懂相关

- 状态：已成稿
- 分类：r-stats
- 成稿：[用 mtcars 看一眼相关与散点](../content/posts/001-mtcars-scatter/001-用mtcars看一眼相关与散点.md)
- 创建：2026-03-18
- 示例数据：待补充

### 选题说明

散点 + cor()，讲清重量和油耗怎么一起动

### 内容大纲


## 007. 啥是正则表达式？为啥字符串处理很关键？

- 状态：已成稿
- 分类：r-base
- 成稿：[啥是正则表达式？为啥字符串处理很关键？](../content/posts/003-regex-real-world/003-啥是正则表达式为啥字符串处理很关键.qmd)
- 创建：2026-09-21
- 示例数据：文章内定义的最小可复现文件名、ICD 编码与固定格式文本；医疗示例仅用于演示字符串规则。

### 选题说明

从批量读取 Excel、识别 ICD 编码和固定格式文本提取三个场景，理解正则表达式如何描述字符串规律。

### 内容大纲

1. **读入所有以 dataset 开头的 xlsx 文件**：用 list.files() 的 pattern 参数筛选文件名，再配合 for 循环逐个读入。
2. **根据 ICD 编码识别呼吸系统感染**：用 grepl() 判断编码是否落在示例规则覆盖的 J00–J22 子范围，并说明规则边界。
3. **从固定格式文本中提取字段**：用捕获组、regexec() 和 regmatches() 提取 ID、日期和值。

### 读者带走

正则表达式先描述文本结构，再帮助 R 批量筛选、判断和提取。
